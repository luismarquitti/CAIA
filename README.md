# CAIA - Central de Artefatos de IA

Sistema de gerenciamento de contexto para desenvolvimento assistido por IA.

## 🚀 Quick Start

1. **Instalar Backend:**
   ```bash
   cd backend
   pip install -r requirements.txt
   fastapi dev main.py
Instalar Frontend:

Bash
cd frontend
npm install
npm run dev